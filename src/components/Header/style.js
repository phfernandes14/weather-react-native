import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    temperatura:{
      fontSize:100
    },
    descricao:{
      fontSize: 20,
      fontStyle: 'italic',
      marginBottom: 35
    }
});